---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - etcd_cf_service_broker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: etcd-cf-service-broker
    version: latest
